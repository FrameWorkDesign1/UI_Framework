package Core.UI.utils.enums;

public enum TestDashboardMetric{
    // once for project
    INSTANCE_RUN_ID,
    PROJECT_ID,
    TRIBE_NAME,
    TRIBE_LEADER,
    SQAD_NAME,

    FRAMEWORK,
    JENKINS_BUILD_NUMBER,
    JENKINS_BUILD_URL,
    JENKINS_JOB_NAME,
    JENKINS_OR_LOCAL,
    // For each scenario
    SCENARIO_NAME,
    ENV,
    ENV_URL,
    CUST_NAME,
    DURATION,
    STATUS,

    TEST_GRID,
    TEST_BROWSER,
    TESTING_TYPE,

    END_TIMESTAMP,
    START_TIMESTAMP,

    TEST_ERROR,
    TEST_ERROR_MESSAGE,
    APPENGG_LEADER,
    QE_LEADER,
    ALLIANCE,
    Custom_Field1,
    Custom_Field2,
    Custom_Field3;
}
