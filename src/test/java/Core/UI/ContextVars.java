package Core.UI;


/* Predefined test context variables */
public enum ContextVars {
    DRIVER,
    WAIT_UTILS,
    SCENARIO,
    BS_LOCAL_SERVER,
    BS_LOCAL_ID,

    // VT: We don't need below vars in context as we are starting tunnel only one for all.
//    LT_LOCAL_SERVER,
//    LT_LOCAL_ID,

    DRIVER_SESSION_ID;
}
